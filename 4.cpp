#include <stdio.h>

int main(){

    int length;
    int width;
    int area;

    printf("length = ");
    scanf("%d", &length);

    printf("width = ");
    scanf("%d", &width);

    printf("area = %d",length * width);

    return 0;
}
